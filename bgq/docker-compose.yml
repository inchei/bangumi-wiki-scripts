services:
  bgq:
    build:
      context: ..
      dockerfile: bgq/Dockerfile
    env_file: .env
    ports:
      - "${PORT:-7860}:${PORT:-7860}"
    volumes:
      - bgq-data:/data
    restart: always

volumes:
  bgq-data:
