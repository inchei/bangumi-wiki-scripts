name: build and release bgq

on:
  push:
    paths:
      - 'bgq/**'
    branches:
      - main
  workflow_dispatch:
  workflow_run:
    workflows: ["update common"]
    types:
      - completed

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            archive_suffix: tar.gz
          - goos: linux
            goarch: arm64
            archive_suffix: tar.gz
          - goos: darwin
            goarch: arm64
            archive_suffix: tar.gz
          - goos: darwin
            goarch: amd64
            archive_suffix: tar.gz
          - goos: windows
            goarch: amd64
            archive_suffix: zip
          - goos: windows
            goarch: arm64
            archive_suffix: zip
    steps:
      - name: 拉取代码
        uses: actions/checkout@v5

      - name: 构建前端
        uses: actions/setup-node@v5
        with:
          node-version: '22'
      - run: |
          cd bgq/frontend
          corepack enable
          pnpm install --frozen-lockfile
          pnpm build
          mkdir -p ../internal/server/dist
          cp -r dist/* ../internal/server/dist/

      - name: 设置 Go
        uses: actions/setup-go@v6
        with:
          go-version-file: bgq/go.mod
          cache-dependency-path: bgq/go.sum

      - name: 构建 bgq
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          cd bgq
          EXT=""
          if [ "${{ matrix.goos }}" = "windows" ]; then
            EXT=".exe"
          fi
          go build -o "bgq${EXT}" ./cmd/bgq/

      - name: 打包
        run: |
          PKG_NAME="bgq-${{ matrix.goos }}-${{ matrix.goarch }}"
          mkdir -p "/tmp/${PKG_NAME}"

          EXT=""
          if [ "${{ matrix.goos }}" = "windows" ]; then
            EXT=".exe"
          fi
          cp "bgq/bgq${EXT}" "/tmp/${PKG_NAME}/"

          cp bgq/README.md "/tmp/${PKG_NAME}/"

          cd /tmp
          if [ "${{ matrix.archive_suffix }}" = "zip" ]; then
            zip -r "${GITHUB_WORKSPACE}/${PKG_NAME}.zip" "${PKG_NAME}/"
          else
            tar czf "${GITHUB_WORKSPACE}/${PKG_NAME}.tar.gz" "${PKG_NAME}/"
          fi

      - name: 上传构建产物
        uses: actions/upload-artifact@v5
        with:
          name: bgq-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            bgq-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz
            bgq-${{ matrix.goos }}-${{ matrix.goarch }}.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: 下载所有产物
        uses: actions/download-artifact@v5
        with:
          path: release-assets
          pattern: bgq-*
          merge-multiple: true

      - name: 清理旧 Release 资产
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release delete latest --yes --cleanup-tag 2>/dev/null || true

      - name: 发布 Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: bgq Release
          body: |
            | 平台 | 文件 |
            |------|------|
            | Linux x86_64 | `bgq-linux-amd64.tar.gz` |
            | Linux ARM | `bgq-linux-arm64.tar.gz` |
            | macOS ARM | `bgq-darwin-arm64.tar.gz` |
            | macOS Intel | `bgq-darwin-amd64.tar.gz` |
            | Windows x86_64 | `bgq-windows-amd64.zip` |
            | Windows ARM | `bgq-windows-arm64.zip` |
          files: |
            release-assets/*.tar.gz
            release-assets/*.zip
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
